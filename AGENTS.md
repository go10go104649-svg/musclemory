# MUSCLEMORY Development Rules

## 最優先ルール

ユーザーから「最優先」と指定された項目は、必ず最初に対応する。

最優先項目が完了するまで、他のUI改善・機能追加・リファクタリングには進まない。

技術的な依存関係や実装上の理由により最初に対応できない場合は、
別作業へ進む前に、その理由をユーザーへ説明する。

## 完了判定ルール

コードを変更しただけでは「完了」としない。

以下をすべて確認してから完了扱いにする。

- 実装が完了している
- ビルドまたは実行時にエラーがない
- 実際の画面・操作で意図した挙動になっている
- 修正対象の不具合が再現しない
- 修正によって既存機能が壊れていない

動作確認できていない場合は、
「実装済み・動作未確認」と明確に報告する。

## 現在の最優先対応

以下4項目が実装・動作確認まで完了するまで、
他の改善には進まない。

1. 最後の1種目まで完全に削除できる

   - トレーニング記録画面で最後に残った1種目も削除できること
   - 最後の種目を削除した後、種目0件の画面になること

2. 種目0件の状態を許可する

   - 種目0件を異常状態として扱わない
   - 種目0件でも画面が正常に表示・操作できること

3. ベンチプレスなどを勝手に再追加しない

   - 種目をすべて削除した後にベンチプレス等が復活しないこと
   - 画面再描画後も復活しないこと
   - 状態更新後も復活しないこと
   - 「種目0件なら初期種目を追加する」処理が残っている場合は削除する

4. 新規トレーニング開始時も種目0件から始める

   - 新規トレーニング開始直後は種目0件であること
   - ベンチプレス等を自動挿入しないこと
   - ユーザーが「種目を追加」から自分で選択すること

## 最優先項目の動作確認

最低限、以下を実際に確認する。

1. 新規トレーニングを開始する
2. 初期状態で種目が0件であることを確認する
3. 任意の種目を追加する
4. 追加した種目を削除する
5. 最後の1種目まで削除できることを確認する
6. 種目0件の状態が維持されることを確認する
7. ベンチプレス等が自動で復活しないことを確認する
8. 画面を再描画・状態更新しても種目0件が維持されることを確認する

この確認が終わるまでは、最優先対応を完了扱いにしない。

## 次の優先対応

最優先4項目が完了した後、以下を順次対応する。

- 丸チェック機能を設定からON/OFFできるようにする
  - ON：チェックUIを表示する
  - OFF：チェックUI自体を非表示にする

- タイマー機能を設定からON/OFFできるようにする
  - ON：タイマーを表示・利用可能にする
  - OFF：タイマーUI自体を非表示にする

- 入力途中のセットを1行単位で削除できるようにする
  - 重量・回数入力途中でも削除可能にする
  - 空欄への上書きではなく、行自体を削除する

- トレーニング中の記録を全削除／リセットできるようにする
  - 「中断」と「削除」を分ける
  - 削除時は確認ダイアログを表示する
  - 削除した記録は履歴や下書きに残さない

- ホーム画面の「今日も積み上げよう」を削除する