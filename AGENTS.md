# MUSCLEMORY Development Rules

## 最優先ルール

ユーザーから「最優先」と指定された項目は、必ず最初に対応する。

最優先項目が完了するまで、他のUI改善・機能追加・リファクタリングには進まない。

技術的な依存関係や実装上の理由により最初に対応できない場合は、
別作業へ進む前に、その理由をユーザーへ説明する。

## 完了判定ルール

コードを変更しただけでは「完了」としない。

以下をすべて確認してから完了扱いにする。

- 実装が完了している
- ビルドまたは実行時にエラーがない
- 実際の画面・操作で意図した挙動になっている
- 修正対象の不具合が再現しない
- 修正によって既存機能が壊れていない

動作確認できていない場合は、
「実装済み・動作未確認」と明確に報告する。

### 3D筋肉グリッド / ヒートマップ

3D人体モデルは、一般的なリアル人体モデルではなく、
全身を細かな格子で表現したワイヤーフレーム / グリッド状の人体モデルとする。

見た目の基準：
- 全身の輪郭と立体感が分かる細かな3Dグリッド
- 人体全体はニュートラルなワイヤーフレーム表示
- トレーニング部位だけを赤系で強調する
- 多色ヒートマップは使用しない
- 色は赤1色のみ使用する
- トレーニング量は赤の濃淡・強さで表現する
- ベタ塗りではなく、細かなドット / 点描状の密度や強さで表現する
- 胸、肩、腕、背中、腹部、脚など、筋肉部位ごとに独立して強度を表示する
- トレーニングしていない部位は赤くしない

強度：
- 初期実装は、選択期間内の部位別セット数を基準にする
- セット数が多い部位ほど赤いドットを濃く・強く表示する
- 単純な総重量や総ボリュームだけでは判定しない

表示期間：
- 1週間
- 1ヶ月
- 3ヶ月
- 6ヶ月
- 1年

将来的には、部位ごとの負荷差を補正した指標に拡張可能な設計にする。

実装・期間集計テスト・iPhoneシミュレーターでの動作確認まで完了済み。

## トレーニング設定・記録画面の改善

マイページ内に「トレーニング設定」という設定画面を作り、
トレーニング記録に関する設定を1か所にまとめる。

### 1. セット完了チェック

- ON / OFFを選択可能にする
- ONの場合は記録画面にチェックUIを表示する
- OFFの場合はチェックUIを非表示にする

### 2. トレーニングタイマー

- ON / OFFを選択可能にする
- OFFの場合はトレーニングタイマーUIを非表示にする

### 3. 筋トレ時間

- ON / OFFを選択可能にする
- トレーニング時間の表示・記録が不要なユーザーはOFFにできるようにする
- OFFの場合は記録画面・結果画面・履歴等に筋トレ時間を表示しない

### 4. 休憩タイマー

休憩タイマーは「セット完了チェック」に紐づく機能として扱う。

- ON / OFFを選択可能にする
- セット完了チェックがONの場合のみ利用可能にする
- セット完了チェックがOFFの場合は休憩タイマーを非表示・無効化する
- セット完了チェック機能自体を廃止する場合は、休憩タイマーも削除する

### 5. 休憩時間

- ユーザーが休憩時間を設定できるようにする
- セット完了チェックがONの場合のみ表示・設定可能にする
- セット完了チェックがOFFの場合は休憩時間の表示・設定・動作をすべて無効化する
- セット完了チェック機能自体を廃止する場合は、休憩時間設定も削除する

### 設定画面の配置

以下の項目をマイページ直下に個別で並べず、
すべて「トレーニング設定」の詳細画面内にまとめる。

- セット完了チェック
- トレーニングタイマー
- 筋トレ時間
- 休憩タイマー
- 休憩時間

実装・設定永続化テスト・iPhoneシミュレーターでの動作確認まで完了済み。

## トレーニングボリューム表示の変更

現在の「t（トン）」表記を廃止し、
トレーニングボリュームはすべてkg表記に統一する。

例：

12.5t
↓
12,500kg

以下を含め、ボリューム表示箇所をすべて確認する。

- ホーム
- 履歴
- トレーニング詳細
- その他ボリュームを表示している画面

kg表記への統一・桁区切りテストまで完了済み。

## 有酸素カテゴリの追加

種目カテゴリに「有酸素」を追加する。

有酸素マシン・有酸素運動を登録できるようにする。

初期候補：

- トレッドミル
- エアロバイク
- クロストレーナー
- ステアクライマー
- ローイングマシン

今後、有酸素種目を追加できる構造にしておく。

初期5種目の追加・カスタム種目カテゴリ対応・集計テストまで完了済み。

## 種目追加画面の整理

現在のように全種目をごちゃ混ぜで一覧表示しない。

種目追加時は、

「部位・カテゴリを選択」
↓
「そのカテゴリの種目を選択」

という2段階構成に変更する。

### 部位・カテゴリ例

- 胸
- 背中
- 肩
- 腕
- 脚
- 腹筋
- 有酸素

### 例

「胸」を選択
↓
以下のような胸種目だけを表示する。

- ベンチプレス
- インクラインベンチプレス
- チェストプレス
- ケーブルフライ

### 目的

- 種目数が増えても一覧がごちゃつかない
- 目的の種目を見つけやすくする
- 初心者でも迷いにくい画面にする

既存の検索機能がある場合は、
カテゴリ選択後の種目一覧でも検索可能にする。

2段階選択・カテゴリ内検索・既存フローの回帰テストまで完了済み。
