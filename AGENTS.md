# SETKEEP

- 既存設計・UI・命名規則を尊重し、既存実装の再利用を優先する。
- iOS / Android両対応。日本語 / 英語対応を維持する。
- 指示範囲外の新機能・大規模リファクタリングを行わない。
- 既存ユーザーのデータ・設定・履歴との互換性を維持する。
- 3Dは既存の共通構成を再利用し、重複と容量増加を抑える。3D作業時のみ `tool/exercise_forms/README.md` の制作・検証手順を確認する。
- 生成ファイル（*.g.dart等）は直接編集せず、生成元を修正して再生成する。
- 変更範囲に応じた解析・テストを行う。文書のみの変更はビルド不要。共通・ネイティブ変更は必要な両OS確認を行う。
- 秘密情報をコード・リポジトリへ含めない。
- 今回の明示的な依頼を最優先する。
- 関連箇所を検索して必要範囲だけ読む。無関係な過去ログ・生成物・QAメディアの一括読込は避ける。
