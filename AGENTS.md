# MUSCLEMORY Development Rules

## 最優先ルール

ユーザーから「最優先」と指定された項目は、必ず最初に対応する。

最優先項目が完了するまで、他のUI改善・機能追加・リファクタリングには進まない。

技術的な依存関係や実装上の理由により最初に対応できない場合は、
別作業へ進む前に、その理由をユーザーへ説明する。

## 完了判定ルール

コードを変更しただけでは「完了」としない。

以下をすべて確認してから完了扱いにする。

- 実装が完了している
- ビルドまたは実行時にエラーがない
- 実際の画面・操作で意図した挙動になっている
- 修正対象の不具合が再現しない
- 修正によって既存機能が壊れていない

動作確認できていない場合は、
「実装済み・動作未確認」と明確に報告する。

## 現在の最優先対応：3D筋肉マネキン

従来のワイヤーフレーム / グリッド人体案は廃止し、筋肉の形と位置が分かる3D筋肉マネキンへ変更する。

目的：
ユーザーが、種目やマシンで使う筋肉を直感的に理解できるようにする。

実装要件：

- 全身の3D筋肉マネキンを表示する
- GLB内で筋肉を名前付きの独立パーツとして保持する
- 大胸筋、三角筋前部・後部、上腕二頭筋、上腕三頭筋、広背筋、僧帽筋、腹直筋、大臀筋、大腿四頭筋、ハムストリングス、下腿筋群などを個別着色できるようにする
- 前面・側面・背面を確認できるようにし、対応端末ではドラッグ回転も利用できる構造にする
- 使用しない筋肉は通常色、対象筋は赤系で表示する
- 多色ヒートマップは使用しない

種目・マシン説明：

- 種目ごとにメインターゲット筋と補助筋を紐づける
- メインターゲット筋は濃い赤で表示する
- 補助筋は薄い赤で表示する
- 例：チェストプレスは大胸筋を濃い赤、三角筋前部と上腕三頭筋を薄い赤で表示する

トレーニング履歴：

- 同じ3Dモデルを使用する
- 1週間、1ヶ月、3ヶ月、6ヶ月、1年を切り替え可能にする
- 初期実装は選択期間内の部位別セット数を基準に赤の濃淡を決める
- 単純な総重量だけで濃さを決めない

実装方式：

- iOSはSceneKit、AndroidはFilamentでローカルGLBを表示する
- 筋肉パーツ名と種目の対応はアプリ側のデータとして管理する
- 3Dアセットはプロジェクト内の生成スクリプトから再生成可能にする
- 権利不明の外部モデルへ依存しない

ローカルGLB、51個の名前付きパーツ、種目別マッピング、履歴の濃淡表示、前面・側面・背面切り替えを実装済み。マッピングテストとiPhoneシミュレーターでの3方向表示・着色確認まで完了済み。

## トレーニング設定・記録画面の改善

マイページ内に「トレーニング設定」という設定画面を作り、
トレーニング記録に関する設定を1か所にまとめる。

### 1. セット完了チェック

- ON / OFFを選択可能にする
- ONの場合は記録画面にチェックUIを表示する
- OFFの場合はチェックUIを非表示にする

### 2. トレーニングタイマー

- ON / OFFを選択可能にする
- OFFの場合はトレーニングタイマーUIを非表示にする

### 3. 筋トレ時間

- ON / OFFを選択可能にする
- トレーニング時間の表示・記録が不要なユーザーはOFFにできるようにする
- OFFの場合は記録画面・結果画面・履歴等に筋トレ時間を表示しない

### 4. 休憩タイマー

休憩タイマーは「セット完了チェック」に紐づく機能として扱う。

- ON / OFFを選択可能にする
- セット完了チェックがONの場合のみ利用可能にする
- セット完了チェックがOFFの場合は休憩タイマーを非表示・無効化する
- セット完了チェック機能自体を廃止する場合は、休憩タイマーも削除する

### 5. 休憩時間

- ユーザーが休憩時間を設定できるようにする
- セット完了チェックがONの場合のみ表示・設定可能にする
- セット完了チェックがOFFの場合は休憩時間の表示・設定・動作をすべて無効化する
- セット完了チェック機能自体を廃止する場合は、休憩時間設定も削除する

### 設定画面の配置

以下の項目をマイページ直下に個別で並べず、
すべて「トレーニング設定」の詳細画面内にまとめる。

- セット完了チェック
- トレーニングタイマー
- 筋トレ時間
- 休憩タイマー
- 休憩時間

実装・設定永続化テスト・iPhoneシミュレーターでの動作確認まで完了済み。

## トレーニングボリューム表示の変更

現在の「t（トン）」表記を廃止し、
トレーニングボリュームはすべてkg表記に統一する。

例：

12.5t
↓
12,500kg

以下を含め、ボリューム表示箇所をすべて確認する。

- ホーム
- 履歴
- トレーニング詳細
- その他ボリュームを表示している画面

kg表記への統一・桁区切りテストまで完了済み。

## 有酸素カテゴリの追加

種目カテゴリに「有酸素」を追加する。

有酸素マシン・有酸素運動を登録できるようにする。

初期候補：

- トレッドミル
- エアロバイク
- クロストレーナー
- ステアクライマー
- ローイングマシン

今後、有酸素種目を追加できる構造にしておく。

初期5種目の追加・カスタム種目カテゴリ対応・集計テストまで完了済み。

## 種目追加画面の整理

現在のように全種目をごちゃ混ぜで一覧表示しない。

種目追加時は、

「部位・カテゴリを選択」
↓
「そのカテゴリの種目を選択」

という2段階構成に変更する。

### 部位・カテゴリ例

- 胸
- 背中
- 肩
- 腕
- 脚
- 腹筋
- 有酸素

### 例

「胸」を選択
↓
以下のような胸種目だけを表示する。

- ベンチプレス
- インクラインベンチプレス
- チェストプレス
- ケーブルフライ

### 目的

- 種目数が増えても一覧がごちゃつかない
- 目的の種目を見つけやすくする
- 初心者でも迷いにくい画面にする

既存の検索機能がある場合は、
カテゴリ選択後の種目一覧でも検索可能にする。

2段階選択・カテゴリ内検索・既存フローの回帰テストまで完了済み。
