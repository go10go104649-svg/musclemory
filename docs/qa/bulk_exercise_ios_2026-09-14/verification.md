# 最優先：複数種目の一括追加（2026-09-14）

実装：マイメニュー／カテゴリの種目をチェックして一括確定。選択件数、全選択、個別解除、全解除を表示。カテゴリ移動で選択を保持。既存種目は追加済みとして選択不可。反映時にも重複を除外。マイメニューの登録順と保存セット内容を引き継ぎ、完了チェックは未完了にする。1件だけ追加する操作も維持。

確認環境：
- iOS 26.5 / iPhone 17仕様の専用MUSCLEMORY QA。
- Android 16 / Pixel 7仕様の専用musclemory_qa。
- 実機は未確認。元のユーザー用iPhoneへの統合テスト／インストールは行っていない。

結果：
- flutter analyze：指摘なし。
- flutter test：83件すべて成功。390×844の小画面での一括追加を含む。
- iOS／Android arm64：ビルド成功。
- 両OSの実画面統合テスト：成功。
- 3種目の同時選択、選択件数、全解除、全選択、個別解除／再選択、一括確定を実操作。
- 登録順、保存したセット数、未完了状態を検査。追加後の重量22.5kg入力が下書きへ反映。
- 同じメニューを開くと3種目とも追加済み。重複追加できない。
- 続けて別の1種目を追加でき、先に入力した重量も保持。

実画面：three_exercises_selected.png、bulk_added_and_weight_edited.png。Android版は ../bulk_exercise_android_2026-09-14/ に保存。

検証用データはテストフィクスチャ。専用端末のみを使い、--keep-app-runningでアプリを保持した。GitHubへのcommit/pushは行っていない。
